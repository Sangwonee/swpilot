나무의 건간지수 H 를 계산하는 공식은 다음과 같습니다.

계수는 이 공식을 발견한 비오네티 박사의 이름을 본따 만들어 졌으며, 그 값은 0.415
이다.

대기압 1atm 일 때, 건강한 생명나무의 생명지수 H 값은 8~10 내외이다.



수행과제

[최종 결과물]
온도, 습도, 산소 농도 값을 사용자로부터 입력 받아 생명나무의 건강지수 H 값을 계산하여출력하는 Java 프로그램을 구현한다.

[과제 목표]
Java의 메서드 구조를 이해하고, 인자 및 반환값을 활용하여 다양한 메서드를 설계하고 구현한다.

[기능 요구사항]

사용자로부터 온도, 습도, 산소 농도 3가지 값을 입력 받는다.

모든 값이 올바르게 입력되면, 주어진 수식을 통해 H 지수를 계산하고 그 결과를 출력한다.

3가지 값 중 하나라도 누락되면, 입력 안내 문구를 출력하고 프로그램을 종료한다.

[구현 지침]

파일의 이름은 HelloBiodome03.java 이다.

온도, 습도, 산소 농도 값을 저장할 각 변수를 생성한다.

원주율 π 값(3.14)을 저장해두는 상수를 생성한다.

사용자가 입력한 값을 각각 올바른 형태의 타입으로 형변환하여 저장한다.

습도값을 인자로 입력 받아 루트 계산 결과를 반환하는 수식(√)을 메서드로 구현한다.

√습도와 온도를 입력 받아 절대값을 계산하고 결과를 반환하는 수식을 메서드로 구현한다.

생명나무의 건강지수를 계산하는 수식을 메서드로 구현한다.

사용자가 입력한 값이 숫자가 아니거나, 값이 3개 모두 입력되지 않으면 재입력 안내 문구가 출력되고 프로그램이 종료된다.

main 함수에서 계산된 최종 결과값 H를 출력한다.

[결과 예시]

25.5 65.0 21.0
-> 생명지수 H = 9.0964172442
3.2 55.1 aa
-> 입력된 값이 올바르지 않습니다. [온도][습도][산소농도] 순서 대로 숫자 값을 입
력해주세요

제약사항

사용자 입력은 Command Line Arguments 을 이용해 받는다.

Scanner , BufferedReader 를 사용할 수 없다.

Java 기본 제공 Math 패키지를 사용할 수 없다.

보너스 과제

계산된 건강지수 H 값을 소수점 셋째 자리에서 반올림하여 출력한다. 

Math 패키지 사용할 수 없다.

for 와 while 반복문을 사용할 수 없다

23.1 60.0 23.0
-> 생명지수 H = 9.23


---


평가 가이드

[기본 확인]

파일명이 HelloBiodome03.java 인지 확인한다.

에러나 예기치 않은 종료없이 프로그램이 실행되어야한다.

[프로그램 구현 확인]

온도, 습도, 산소 농도 값을 저장할 각 변수를 적절한 숫자 데이터 타입으로 생성했는지확인한다.

float, double

원주율 π 값(3.14)을 저장해두는 상수를 final 로 생성했는지 확인한다.

사용자가 입력한 값을 각각 올바른 숫자 타입으로 형변환하여 저장하는지 확인한다.

숫자 인자로 입력 받아 숫자값을 반환하는 루트 계산을 하는 수식(√)을 메서드로 구현했는지 확인한다.

숫자값을 인자로 입력 받아 계산한 절대값을 반환하는 수식을 메서드로 구현했는지 확인한다.

나무의 최종 건강지수를 계산하는 수식을 메서드로 구현했는지 확인한다.

[프로그램 동작 확인]

사용자가 숫자값 3개를 입력하면 계산된 값이 출력되어야한다.

수식에 따라 계산한 값이 올바른지 반드시 확인한다.

java HermersOfBiodome03 25.5 65.0 21.0

생명지수 H = 9.0964172442

사용자가 3개 입력값 모두 올바르게 넣지 않았을 때 올바른 입력 방법을 안내하고 프로그램이 종료해야한다.

java HermersOfBiodome03 13.2 55.1 aa

올바른 값이 입력되지 않았습니다. [온도][습도][산소농도] 순서 대로 숫자 값을 입력해주세요

[개념 확인 질문]

온도, 습도, 산소 농도를 저장하는 데이터 타입을 선택한 이유를 질문한다.

bye, int, short, long, float, double 데이터의 범위, 메모리 사용, 계산의 효율성을 근거로 답변한다.

변수와 상수의 개념에 대해 질문한다.

프로그래밍시 변수와 상수의 차이점은 무엇이며, 두개를 구분해서 사용하는 필요성은 무엇인가?

본 문제에서 어떤 값이 변수이고 상수인지 질문한다.

메서드의 구현과 활용에 대해 질문한다.

메서드를 사용하는 주된 이유는 무엇이라고 생각하는가?

본 문제에서 구현한 수식 메서드의 각 파라미터와 반환값을 설명하시오.